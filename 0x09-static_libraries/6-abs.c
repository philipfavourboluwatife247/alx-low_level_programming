#include "main.h"

/**
 * _abs - compute absolute value of int
 * @n: Integer to be computed
 * Return: absolute value of integer
 */
int _abs(int n)
{
        if (n >= 0)
                return (n);

        else
                return (-n);
}
